[package]
name = "hocuspocus-common"
version = "4.1.0"
edition = "2021"
description = "Shared types and utilities for hocuspocus-rs (Rust port of @hocuspocus/common)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
